## This repository contains a set of tools in order make creating a new work environ on linux faster and easier

Currently this repository contains the following :
- a bashrc file that contains a nice PS1 ( the command line prompt ) and a few aliases
- a tools.txt file containing useful tools to install
